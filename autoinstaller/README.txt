To use the autoinstaller download the autoinstaller directory; 

run the autoinstaller exe and locate to your munch directory, select the munch directory; this will then backup your original munch.exe and glew32.dll before replacing them with the patch;

to undo the patch run the autoinstaller again and type y for the backup loader prompt.